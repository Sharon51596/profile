# Portfolio 作品集網站

這是一個使用 Next.js 和 TypeScript 建立的現代化作品集網站。

## 開始使用

### 安裝依賴

```bash
npm install
```

### 開發模式

```bash
npm run dev
```

在瀏覽器中打開 [http://localhost:3000](http://localhost:3000) 查看網站。

### 建置生產版本

```bash
npm run build
npm start
```

## 專案結構

```
Profolio/
├── app/                    # Next.js App Router
│   ├── layout.tsx         # 根布局
│   ├── page.tsx           # 首頁
│   └── globals.css        # 全域樣式
├── components/            # React 組件
│   ├── Header.tsx         # 導航欄
│   ├── Hero.tsx           # 首頁英雄區塊
│   ├── About.tsx          # 關於我區塊
│   ├── Projects.tsx       # 作品展示區塊
│   ├── Contact.tsx        # 聯絡區塊
│   └── Footer.tsx         # 頁尾
├── package.json           # 專案依賴
├── tsconfig.json          # TypeScript 配置
└── next.config.js         # Next.js 配置
```

## 自訂內容

### 1. 更新個人資訊
- 編輯 `components/About.tsx` 修改關於我的內容
- 編輯 `components/Hero.tsx` 修改首頁標題和介紹

### 2. 添加作品
- 編輯 `components/Projects.tsx` 中的 `projects` 陣列
- 添加專案圖片到 `public/` 資料夾
- 更新專案連結和描述

### 3. 更新聯絡方式
- 編輯 `components/Contact.tsx` 更新 Email、GitHub、LinkedIn 連結

### 4. 修改樣式
- 每個組件都有對應的 `.module.css` 檔案
- 可以修改顏色、字體、間距等樣式

## 技術棧

- **Next.js 14** - React 框架
- **TypeScript** - 類型安全
- **CSS Modules** - 樣式管理
- **React 18** - UI 庫

## 下一步建議

1. ✅ 專案基礎結構已建立
2. 📝 更新個人資訊和作品內容
3. 🎨 自訂顏色和樣式主題
4. 🖼️ 添加真實的專案圖片
5. 📱 優化行動裝置體驗
6. 🚀 部署到 Vercel 或其他平台

## 部署

最簡單的部署方式是使用 [Vercel](https://vercel.com)：

1. 將專案推送到 GitHub
2. 在 Vercel 中導入專案
3. 自動部署完成！

---

祝你建立一個出色的作品集網站！🎉

